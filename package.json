{
  "name": "pursetoken",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ pursetoken",
    "create-local": "graph create --node http://localhost:8020/ pursetoken",
    "remove-local": "graph remove --node http://localhost:8020/ pursetoken",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 pursetoken",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.82.0",
    "@graphprotocol/graph-ts": "0.35.1"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}
